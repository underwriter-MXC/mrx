/**
 * SpeakableSpecification factory. The §7-disclaimer-pointed spec lives
 * in its own file so the architecture's AEO protection is explicit.
 * Per SEO plan §2.5.
 */
export { speakable } from './article';
